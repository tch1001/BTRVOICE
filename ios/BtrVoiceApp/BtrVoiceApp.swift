// The containing iOS app: record, review, and explicitly commit dictated text.

import SwiftUI

@main
struct BtrVoiceApp: App {
  var body: some Scene {
    WindowGroup {
      ContentView()
        .tint(Color(red: 0.36, green: 0.42, blue: 0.99))
    }
  }
}
